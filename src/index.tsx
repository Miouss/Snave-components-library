import ReactDOM from "react-dom/client";
import { StackDrawingImgs } from "./StackDrawingImgs";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLDivElement).render(
  <StackDrawingImgs />
);
